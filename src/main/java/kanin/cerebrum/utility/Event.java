package kanin.cerebrum.utility;

//This enum is essentially just a grouping enum of all the supported events
public enum Event {
    MOUSE_MOVE,
    MOUSE_CLICK,
    MOUSE_HOLD,
    MOUSE_RELEASE,
    KEY_PRESS,
    KEY_RELEASE,
    SCROLL,
}
